spring:
  application:
    name: cavy-workflow-boot
  cloud:
    nacos:
      config:
        namespace: public
        contextPath: /nacos
        server-addr: 192.168.2.2:8848
        password: nacos
        username: nacos
      #        shared-configs[0]:
      #          data-id: redis.yml
      #          group: DEFAULT_GROUP
      #          refresh: true
      #        shared-configs[1]:
      #          data-id: file.yml
      #          group: DEFAULT_GROUP
      #          refresh: true
      #        file-extension: yml
      discovery:
        server-addr: 192.168.2.2:8848
        namespace: public
        password: nacos
        username: nacos